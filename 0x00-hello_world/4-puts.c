#include <stdio.h>
/**
 * main - Entry  main point
 * Description - c program uses the puts function to print a string followed by a newline.
 * return - 0 for success, non-zero for failure
*/

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return 0;
}
